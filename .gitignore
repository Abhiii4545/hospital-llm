data/real/
# ^ LINE ONE, DELIBERATELY, and before any other file in this repo was written.
#   Real patient documents never enter version control. See docs/PRIVACY.md.
#   Enforced independently by tools/hooks/block_real_data.py, because `git add -f`
#   overrides this file and a hook does not.
data/real/**
**/data/real/

# --- python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# --- project artefacts (regenerable, and large) ---
data/synthetic/
data/cache/
outputs/
checkpoints/
wandb/
*.onnx
*.safetensors
*.ckpt
*.pt

# --- local ---
.env
.DS_Store

# phase 2 mini-set: regenerated from reckon/data/mini_set.py, not committed
data/mini/

# reports generated from a dirty tree are not reproducible, so not committed
reports/*-dirty.md

# scratch renders from development
data/_smoke/
pw_test.png
